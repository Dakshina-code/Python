#datetime
import datetime

currentDate = datetime.date.today();
print(currentDate);
print(currentDate.year);
print(currentDate.month);
print(currentData.day);

#In python we use strftime to format dates
import datetime
currentDate = datetime.date.today();
# ///////////////////////////////////////////////////////////////
print(current.Date.strftime('%d %b,%Y'));
